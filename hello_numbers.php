<?php
    // function sayHi(int $fvar) {
        /* Single Quotes not acceptible for 
        * inputting vars in strings */
        // echo "$fvar Hello World!";
    // }

    // sayHi("Brian");
    function addNumbers(int $a, $b) {
        return $a + $b;
    }

    echo addNumbers(5, "5 days");
?>
